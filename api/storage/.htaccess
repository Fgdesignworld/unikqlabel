# ============================================================
# SECURITY: Deny all web access to storage directory
# ============================================================
# This directory contains security logs, rate limit data, etc.
# Nothing here should ever be accessible via HTTP.

Require all denied

# Fallback for older Apache versions
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
